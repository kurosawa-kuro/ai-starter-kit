# AIによる事務処理・プログラミング自動化 環境構築ガイド

～ Claude Code / Codex CLI を Windows で動かすための WSL + VSCode セットアップ ～

> 🚀 **「とにかく早く動かしたい」方** → [`00_クイックスタート.md`](./00_クイックスタート.md)（15 分版）へどうぞ。
> 本ガイドは **「仕組みも含めて理解しながら進める」** ための詳しい版です（30〜60 分）。

## このガイドの構成（読み方）

```
   📖 全体は大きく 3 ブロック
   ──────────────────────────────────────────────────
   A. 概念の理解（補足、急ぐ方は飛ばしてOK）
      ・このガイドの目的 / WSL とは / 用語の整理 / 図解
      
   B. 実際の手順（必須）
      ・PowerShell とは → ① WSL → ② VSCode → ③ WSL拡張
      → ④ WSL 接続 → ⑤ Git → ⑥ Python 確認
      
   C. 完了確認とトラブルシューティング
      ・Phase 1 完了チェックリスト
      ・症状別の対処
   ──────────────────────────────────────────────────
   ⚡ 急ぐ方: B から読み始める／クイックスタートへ移る
   📚 じっくり派: A から順に読む
```

## 本ガイドのスコープ（最初に把握してください）

本ガイドは **「環境構築フェーズ（git clone する直前まで）」のみ** を扱います。`git clone` 以降の AI ツール導入・実作業は **別ドキュメント [`02_ハンズオン.md`](./02_ハンズオン.md)** を参照してください。

```
┌─────────────────────────────────────────┐         ┌─────────────────────────────────────┐
│  【Phase 1】 環境構築 ← 本ガイドの範囲     │         │  【Phase 2】 ハンズオン ← 別ドキュメント │
│                                          │         │                                      │
│  ・WSL + Ubuntu のインストール             │  ───▶  │  ・git clone でリポジトリ取得         │
│  ・VSCode + WSL拡張のセットアップ          │         │  ・同梱の setup-*.sh を必要分だけ実行 │
│  ・Git のインストール                      │         │  ・AIツール起動 → プロンプトメモ活用   │
│  ・Python の存在確認                       │         │                                      │
│                                          │         │   → AIによる事務処理・プログラミング   │
│                                          │         │      自動化_ハンズオン.md             │
└─────────────────────────────────────────┘         └─────────────────────────────────────┘
       一度やればOK（本ガイドのゴール）                     業務ごとに繰り返し
```

> 💡 つまり本ガイドは、**「git clone してプロンプトを使えば即動く状態」までPC環境を整える**ためのものです。実作業の手順は [`ハンズオン`](./02_ハンズオン.md) ドキュメントに集約しています。

## このガイドの目的

非ITエンジニア（基本的なWindows操作ができる方）が、**AIコーディングツール（Claude Code、Codex CLI 等）を活用して、次の2つを自動化できるようにする**ための環境構築ガイドです。

| 自動化の対象 | 例 |
| --- | --- |
| **① 事務処理の自動化** | Excel集計、ファイル整理、PDF処理、メール作成、Web情報の収集 など |
| **② プログラミングの自動化** | Pythonスクリプトの設計・実装・修正・テスト、Git操作、ライブラリ導入 など |

この2つは **目的も使い方も別物** ですが、どちらも同じ AIツール + WSL + VSCode の環境上で実現できます。

### ゴール

   - **事務処理**を AI に頼んで自動化できる（コードが書けなくても、日本語で指示すればOK）
   - **プログラミング作業**そのものを AI に書かせて／直してもらいながら進められる
   - Windows PC1台で、両方の用途を同じ環境で扱える

### なぜ WSL や VSCode が必要なのか（手段の位置づけ）

AIツール（Claude Code・Codex CLI）も、それが動かす Python などのプログラムも、**Linux 環境で動かすのが世界標準**です。
そのため、Windows PC上で本格的にAIを使うには次の準備が必要です：

| 必要なもの | 役割 | 本ガイドでの位置づけ |
| --- | --- | --- |
| **WSL（+ Ubuntu）** | Windowsの中にLinux環境を用意する | **本ガイドで手動セットアップ**（AI動作の土台） |
| **VSCode** | コードやファイルを編集する窓口 | **本ガイドで手動セットアップ**（操作の入口） |
| **Git** | プロンプト・コード一式を `git clone` で取得 | **本ガイドで手動セットアップ**（運用フェーズの入口） |
| **Python** | AIが書くコードを実行する言語 | **存在確認のみ**（詳細は clone 後のシェルが自動構築） |
| **Claude Code / Codex CLI / nvm / Node.js / Python 3 / VSCode 推奨拡張 / Docker（任意）** | AIツール本体および動作に必要な一式 | **本ガイドの対象外**（[`ハンズオン`](./02_ハンズオン.md) で `git clone` 後、リポジトリ同梱の `src/scripts/setup-*.sh` を必要分実行して導入） |

> 💡 本ガイドで人手でやるのは、**WSL・VSCode・Git・Python確認** の最低限のみ。
> あとは [`ハンズオン`](./02_ハンズオン.md) ドキュメントの手順に従って `git clone` → 同梱シェル実行 で、Claude Code や Codex CLI を含む AI環境一式が自動構築されます。

---

## 用語の整理（最初に読んでください）

| 用語 | 読み方 | 意味 |
| --- | --- | --- |
| **Linux** | リナックス | **プログラムが最適に動作するように設計されたOS**。日常作業ではなく開発・サーバー用途の世界標準。Windowsと同じ「PCの土台ソフト」のひとつ |
| WSL | ダブリュー・エス・エル | Windows Subsystem for Linux の略。**Windows上でLinuxを動かす公式の仕組み** |
| Ubuntu | ウブントゥ | Linuxの種類のひとつ。WSLで使う標準のLinux |
| VSCode | ブイエスコード | Visual Studio Code。マイクロソフト製の無料エディタ |
| ターミナル | - | 文字でPCに命令を送るための画面（黒い画面） |
| PowerShell | パワーシェル | Windows標準のターミナルアプリ |
| ディストリビューション | - | Linuxの種類のこと（Ubuntu、Debianなど）。「ディストロ」とも略す |

---

## Windows / WSL / Ubuntu / VSCode / Python の関係

「なんでこんなに色々なものを入れるの？」を、**エクセルや普段使うアプリと比較して**説明します。

### まず大前提：Linux とは何か

> **Linux = プログラムが最も効率よく動作するように設計されたコンピュータOS。**
> 日常のオフィス作業（メール・表計算・資料作成）用ではなく、**プログラムを動かすこと自体に最適化されている**OSです。

そのため：

   - 世界中の **サーバー・クラウド・スーパーコンピュータの大半は Linux** で動いている
   - **AI・機械学習・Web開発のツールは Linux 前提** で作られているものがほとんど
   - プログラミングをするなら、Linux環境はほぼ必需品

→ だから「Windowsの中にLinuxを置く仕組み（＝WSL）」が重要になります。

### 普段の世界とプログラミングの世界の対応関係

| 普段の世界 | プログラミングの世界 | 役割 |
| --- | --- | --- |
| **Windows** | **Windows**（同じ） | PCのOS（土台） |
| **エクセル** | **VSCode** | 中身を編集するアプリ |
| **`.xlsx` ファイル** | **`.py`（Pythonコード）など** | 編集する対象のファイル |
| **エクセルの数式・関数** | **Python（プログラミング言語）** | 計算や処理をする中身 |

つまり、**「VSCodeはエクセルみたいなもの、Pythonはエクセルの関数みたいなもの」** と思ってOKです。

### Windows と Linux の違い（図1）

冒頭で説明したとおり、**WindowsもLinuxも同じ「OS」** です。違いは「**何に最適化されているか**」だけ。

```
┌─── Windows ──────────────┐     ┌─── Linux（Ubuntu）─────────┐
│                           │     │                             │
│  💻 デスクトップ画面       │     │  ⌨️  ターミナル（黒い画面）  │
│  🖱️ マウスでアイコン操作   │     │  📜 キーボードで文字コマンド │
│                           │     │                             │
│  使う人：一般ユーザー       │     │  使う人：開発者・サーバー管理者│
│  例：エクセル、Word、Edge   │     │  例：Python、Git、AIツール   │
│                           │     │                             │
│  特徴：見やすい・直感的     │     │  特徴：高速・軽量・自由度高い │
└──────────────────────────┘     └────────────────────────────┘
            ↓                                  ↓
        どちらも「OS」（パソコンを動かす基本ソフト）
            ↓
  ⭐ プログラミングの世界の「標準」は Linux 側
  ⭐ なので Windowsの中で Linux も使えると便利！
            ↓
  そのための仕組みが **WSL** です
```

**Ubuntu** は数あるLinuxの種類（ディストリビューション）のひとつで、初心者向けで一番ポピュラー。WSLで標準的に使われます。

### では「WSL」って何？

> エクセルなら計算機能はエクセル自体に入っていますが、
> プログラミングの世界では **「計算するための環境（Linux/Ubuntu）」を別に用意** する必要があります。

なぜかというと：

   - プログラミングのツール（Python・Git・AIツールなど）の多くは **Linux（Ubuntu）の上で動く前提** で作られている
   - Windowsに直接入れると相性問題でうまく動かないことが多い
   - そこで、**Windowsの中に「小さなLinux PC」を一台置く** という仕組みが用意された
   - それが **WSL（Windows Subsystem for Linux）** という、Microsoftが公式に提供する仕組み

### 全体イメージ図（構造）

```
┌─────────────────────────────────────────────────────────────┐
│  Windows（あなたのPC）                                        │
│                                                               │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│   │  エクセル      │  │  VSCode       │  │  WSL              │  │
│   │              │  │              │  │  （Windowsの中の    │  │
│   │  .xlsxを編集  │  │  .pyを編集    │  │   小さなLinux PC）  │  │
│   │              │  │              │  │                    │  │
│   │              │  │       ◀──────┼──┤   Ubuntu          │  │
│   │              │  │   WSL拡張で接続 │   ├────────────────┐│  │
│   │              │  │              │  │   │ Python        ││  │
│   │              │  │              │  │   │ Git           ││  │
│   │              │  │              │  │   │ Claude Code   ││  │
│   │              │  │              │  │   │ Codex CLI 等  ││  │
│   │              │  │              │  │   └────────────────┘│  │
│   └──────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
       ↑                  ↑                    ↑
   Windowsアプリ       Windowsアプリ       Windowsの中に
  （普通にインストール）（普通にインストール） 「もう一台のPC」を置く
```

### 操作の流れ（あなたが何かをした時に何が起きるか）（図2）

「VSCodeで `python hello.py` を実行する」という1回の操作で、裏では次のことが起きています。

```
   👤 あなた
    │
    │ ① キーボード・マウスで操作
    ▼
 ┌────────────────────────────────────────────┐
 │  💻 Windows画面の中の VSCode                 │
 │                                              │
 │   ┌────────────────────────────────┐       │
 │   │ 📝 エディタ                     │       │
 │   │   print("hello")               │       │
 │   └────────────────────────────────┘       │
 │   ┌────────────────────────────────┐       │
 │   │ ⌨️  ターミナル                  │       │
 │   │   $ python hello.py            │ ← ②入力 │
 │   └────────────────────────────────┘       │
 └─────────────────────┬──────────────────────┘
                       │
                       │ ③ WSL拡張機能が
                       │   Windows ↔ Ubuntu を橋渡し
                       ▼
 ┌────────────────────────────────────────────┐
 │  🐧 WSL の中の Ubuntu（Linux）              │
 │                                              │
 │   ┌────────────────────────────────┐       │
 │   │ Python が hello.py を実行       │ ← ④処理 │
 │   │   "hello" という結果を作る       │       │
 │   └────────────────────────────────┘       │
 └─────────────────────┬──────────────────────┘
                       │
                       │ ⑤ 結果を VSCode に返す
                       ▼
 ┌────────────────────────────────────────────┐
 │  💻 VSCode のターミナルに「hello」と表示       │
 └─────────────────────┬──────────────────────┘
                       │
                       ▼
                     👤 あなたの目に見える
```

ポイント：

   - **見えているのは「VSCodeの画面」だけ**（だからエクセルを使うのと同じ感覚で操作できる）
   - **実際に動いているのは Ubuntu（Linux）側**
   - 両者を **WSL拡張機能** が自動で橋渡ししてくれるので、ユーザーは意識しなくてOK

### よくある疑問

**Q. なぜVSCodeから直接Windows側のPythonを使わないの？**
A. Windowsに直接Pythonを入れると、ネット上のサンプルコードや手順書（多くがLinux向け）がそのまま動かないことが多く、トラブルが起きやすいです。Ubuntuに入れれば「世界中の人と同じ環境」になるので情報が見つけやすく、AIツール（Claude Code・Codex CLI）も正常に動きます。

**Q. VSCodeはどこにインストールするの？**
A. **VSCode本体はWindowsアプリ** として普通にインストールします（エクセルと同じ）。それに「WSL拡張機能」を追加することで、Ubuntu側のファイルも編集できるようになります。

**Q. ファイルはどこに保存される？**
A. プログラミング作業のファイルは **Ubuntu内（`/home/ユーザー名/`）** に保存します。エクセルでいう「ドキュメントフォルダ」のような場所がUbuntu内にある、というイメージです。

> 💡 **覚え方**：
> - **Windows = PC本体**
> - **VSCode = エクセルのようなアプリ（窓口）**
> - **WSL + Ubuntu = Windowsの中に置いた“もう一台のLinux PC”（作業場）**
> - **Python など = その作業場で使う道具**

---

## 全体の流れ（本ガイドの範囲）

```
【Phase 1：環境構築（本ガイドの範囲）】

① WSL（Ubuntu）をインストール          ← 必須
       ↓
② VSCodeをインストール                 ← 必須
       ↓
③ VSCodeに「WSL拡張機能」を追加         ← 必須
       ↓
④ VSCodeからWSLに接続できるか確認       ← 必須
       ↓
⑤ Git（WSL内・clone用）を設定           ← 必須（プロンプト取得に使う）
       ↓
⑥ Python（WSL内）の存在確認              ← 必須

  ─── ここまでが本ガイドのゴール ───
  ─── 続きは 02_ハンズオン.md ───
```

> ※ 本ガイドで人手で行うのは ①〜⑥ のみ。
> ※ AIツール（Claude Code・Codex CLI 等）の導入と起動は **[`ハンズオン`](./02_ハンズオン.md)** ドキュメントに分けています。

### どこで作業するか（ステップ別マップ）

ステップごとに「どの画面／どのターミナル」で操作するかが切り替わります。間違えると動かないので注意。

```
   ステップ        どこで作業するか
   ──────────  ──────────────────────────────────────────
   ①            🪟 Windows の PowerShell（管理者）
                 　└─ wsl --install を叩く
                 
   ②            🪟 Windows の Web ブラウザ + インストーラ
                 　└─ VSCode をダウンロードしてインストール
                 
   ③            💻 VSCode の中（拡張機能タブ）
                 　└─ "WSL" 拡張機能を Install
                 
   ④            💻 VSCode → 左下「><」→ "Connect to WSL"
                 　└─ ここで WSL: Ubuntu 表示になる
                 
   ⑤            🐧 WSL 接続後の VSCode ターミナル
                 　└─ git --version など bash コマンド
                 
   ⑥            🐧 WSL 接続後の VSCode ターミナル
                 　└─ python3 --version
   
   📌 大事なポイント:
      - ①は「Windows 側」 → 黒いウィンドウ（PowerShell）
      - ⑤⑥は「WSL の中（Linux）」 → VSCode 左下が "WSL: Ubuntu" 状態
        になっていることを毎回確認
```

---

## 📌 補足: PowerShell とは（① の前に読む）

ステップ ①〜⑥ のうち、**①（WSL インストール）** と **④（WSL 接続前の状態確認）** で `PowerShell` というアプリを使います。普段の業務では使わないアプリなので軽く説明します。

### PowerShell の正体

> **PowerShell ＝ Windows に最初から入っている「文字でコマンドを打って Windows を操作するためのアプリ」**

エクセルがマウスで表を操作するアプリなのに対し、PowerShell は **キーボードでコマンドを打って Windows 全体を操作する** ためのアプリ、というイメージです。

```
   普段の世界                         PowerShell の世界
   ──────────────────────            ──────────────────────────
   📊 エクセルで                      📜 PowerShell で
      数式を入力 → 計算結果              wsl --install と打つ → WSLが入る
      （マウス＋数式）                   （キーボード＋コマンド）
```

### 起動の仕方（2 通り）

| 起動方法 | やり方 | こういう時に使う |
|---|---|---|
| **普通起動** | スタートを右クリック →「ターミナル」 or 「Windows PowerShell」 | 状態確認系（`wsl -l -v` など）|
| **管理者起動** | スタートを右クリック →「**ターミナル（管理者）**」or「**Windows PowerShell（管理者）**」 | システムを変更する系（`wsl --install` など）。クリック後に「変更を許可しますか？」と聞かれる |

### 見分け方

```
   ✅ 管理者で起動できた PowerShell
   ┌──────────────────────────────────────────────────┐
   │ Administrator: Windows PowerShell                │  ← タイトルに「Administrator」
   │                                                  │     または「管理者」が表示
   │ PS C:\Windows\system32>                          │  ← 開始フォルダが
   │                                                  │     system32 のことが多い
   └──────────────────────────────────────────────────┘
   
   ❌ 普通に起動した PowerShell（① では NG）
   ┌──────────────────────────────────────────────────┐
   │ Windows PowerShell                                │  ← 「Administrator」が無い
   │                                                  │
   │ PS C:\Users\<あなた>>                              │  ← 開始フォルダが Users
   │                                                  │
   └──────────────────────────────────────────────────┘
```

> 💡 ① の `wsl --install` は **管理者起動** が必須。普通起動だと権限不足で失敗します。
> ④ 以降の `wsl -l -v` などは **普通起動** で OK。

### PowerShell ≠ WSL のターミナル

混同しやすいポイント。両者は **別物** です。

```
   🪟 PowerShell（Windows 側）
   ・Windows のコマンド（wsl --install / wsl -l -v 等）を実行
   ・C ドライブ、Windows のファイルが見える
   
        ──── ここからは別の世界 ────
   
   🐧 WSL の中（Linux 側）
   ・Linux のコマンド（git / python3 / bash 等）を実行
   ・WSL 内ホーム（/home/...）が「自分の場所」
   ・⑤⑥ でやる作業はこちら
```

ステップ ①②④冒頭は **🪟 PowerShell**、⑤⑥は **🐧 WSL の中**（VSCode から接続したターミナル）と覚えてください。

---

## ① WSL（Ubuntu）のインストール

### 前提条件
- Windows 10 バージョン2004以降（ビルド19041以降）または Windows 11
- 管理者権限のあるアカウントでログインしていること

### 手順

**1. PowerShellを「管理者として」起動する**
   - 画面左下の「スタート」ボタンを右クリック
   - メニューの中から **「ターミナル（管理者）」** または **「Windows PowerShell（管理者）」** を選択
   - 「このアプリがデバイスに変更を加えることを許可しますか？」と聞かれたら **「はい」** をクリック

**2. 以下のコマンドを入力してEnterキーを押す**

```powershell
wsl --install
```

   - 自動的にUbuntuがインストールされます
   - 完了したら **PCを再起動** してください

**3. 再起動後、Ubuntuが自動起動する**
   - 黒い画面が開き、しばらく待つとユーザー名とパスワードの入力を求められます
   - **ユーザー名**：好きな英小文字（例：`taro`）
   - **パスワード**：好きな文字列（入力しても画面に表示されませんが、ちゃんと入力されています）
   - パスワードは2回入力します

**4. インストールが成功したか確認**
   - PowerShell（管理者じゃなくてOK）を開いて以下を実行

```powershell
wsl -l -v
```

   - `Ubuntu` が表示され、`STATE` が `Running` または `Stopped`、`VERSION` が `2` になっていれば成功

### 主なWSLコマンド

| コマンド | 用途 | 主な使用場面 | 注意点 |
| --- | --- | --- | --- |
| `wsl --install` | WSLの新規インストール | 初回セットアップ時 | 管理者PowerShellで実行 |
| `wsl` | 既定ディストリビューション起動 | 通常起動 | 既定設定が必要 |
| `wsl -l -v` | インストール済み一覧＋状態表示 | 状態確認・WSLバージョン確認 | `--list --verbose`の短縮形 |
| `wsl --shutdown` | WSL全体停止 | メモリ解放・再起動前 | 全ディストリビューション停止 |
| `wsl --unregister Ubuntu-22.04` | 指定ディストリビューション削除 | 環境リセット時 | データ完全削除（復元不可） |

---

## ② VSCodeのインストール

### 手順

**1. ダウンロードページを開く**
   - ブラウザで以下のURLにアクセス
   - https://code.visualstudio.com/

**2. ダウンロードボタンをクリック**
   - 「Download for Windows」と書かれた青いボタンをクリック
   - インストーラ（`.exe`ファイル）がダウンロードされます

**3. インストーラを実行**
   - ダウンロードした `VSCodeUserSetup-x64-x.x.x.exe` をダブルクリック
   - 「使用許諾契約書の同意」→ **「同意する」** を選択
   - インストール先はそのままで **「次へ」**
   - 「追加タスクの選択」では以下にチェックを入れることを推奨
     - ☑ **「PATHへの追加」**（既定でチェック済み）
     - ☑ 「エクスプローラーのファイルコンテキストメニューに〜」
     - ☑ 「エクスプローラーのディレクトリコンテキストメニューに〜」
   - 「インストール」をクリック → 完了

**4. VSCodeを起動**
   - スタートメニューから「Visual Studio Code」を検索して起動
   - 初回起動時にテーマや言語の選択画面が出ます（後から変更可能）

### 日本語化（任意）

   - 左サイドバーの **「拡張機能」アイコン**（四角が4つのマーク）をクリック
   - 検索欄に `Japanese` と入力
   - 「Japanese Language Pack for Visual Studio Code」を **Install**
   - 右下に「Restart」ボタンが出るのでクリックして再起動

> 💡 日本語化を含む VSCode 推奨拡張は、ハンズオン側で `setup-vscode-extensions.sh` により一括導入もできます（Phase 2）。今ここで日本語化だけ手動で入れておいても、後でシェルを実行しても、どちらでも構いません。

---

## ③ VSCodeに「WSL拡張機能」を追加

### 手順

**1. VSCodeを起動**

**2. 左サイドバーの「拡張機能」アイコンをクリック**
   - 四角が4つ並んだマーク（上から4〜5番目）

**3. 検索欄に `WSL` と入力**

**4. 「WSL」（発行者：Microsoft）を選択**
   - ![Microsoftの公式マーク]がついていることを確認
   - **「Install」** ボタンをクリック

**5. インストール完了**
   - 画面左下に **「><」** のような緑色のアイコンが出れば成功

---

## ④ VSCodeからWSL接続確認

### VSCode の左下アイコンの見分け方（図）

このステップでは VSCode 画面の **左下のアイコン表示** が変わるかを見ます。

```
   ❌ 接続前の VSCode（Windows 側にいる状態）
   ┌──────────────────────────────────────────┐
   │                                          │
   │              VSCode の画面                │
   │                                          │
   │                                          │
   │                                          │
   │                                          │
   ├──────────────────────────────────────────┤
   │ >< 　メイン　 ✓ 0 ⚠ 0      ← アイコンだけ │  ← 緑の「><」だけ
   └──────────────────────────────────────────┘

                       ↓ 緑「><」クリック → "Connect to WSL"

   ✅ 接続後の VSCode（WSL の中で作業している状態）
   ┌──────────────────────────────────────────┐
   │                                          │
   │              VSCode の画面                │
   │                                          │
   │                                          │
   │                                          │
   │                                          │
   ├──────────────────────────────────────────┤
   │ >< WSL: Ubuntu　 メイン　✓ 0 ⚠ 0          │  ← "WSL: Ubuntu" 表示
   └──────────────────────────────────────────┘

   📌 「WSL: Ubuntu」が出ていない＝まだ Windows 側で作業している
      この状態で bash コマンドを叩いても WSL 側には届きません
```

### 手順

**1. VSCodeを起動した状態で、画面左下の緑色「><」アイコンをクリック**

**2. 上部に表示されるメニューから「Connect to WSL」を選択**
   - 「WSLへの接続」と表示されることもあります

**3. 接続中の表示を待つ**
   - VSCodeが自動的に再起動されます
   - 左下のアイコン表示が **「WSL: Ubuntu」** に変われば接続成功

**4. ターミナルで動作確認**
   - メニューバー：「ターミナル」 → 「新しいターミナル」
   - または **`Ctrl + @`** （Ctrl + Shift + @ の場合あり）
   - 開いたターミナルで以下を実行

```bash
uname -a
```

   - `Linux ...` で始まる文字列が表示されればWSL内で動作しています

**5. ファイル操作の確認（任意）**
   - 「ファイル」 → 「フォルダーを開く」
   - 上部のパス欄に `/home/ユーザー名/` と入力してEnter
   - WSL内のホームディレクトリが開けば、本格的に開発を始められます

---

## ⑤ Git（WSL内・clone用）の設定 【必須】

ハンズオンの最初に **セットアップシェル・プロンプトメモ・サンプルデータ一式を `git clone` で取得する** ため、Git のセットアップは必須です。

WSLに接続した状態のVSCodeターミナルで実行します。

**1. インストール確認**

```bash
git --version
```

   - 通常は最初からインストールされています。バージョンが出ればOK
   - 出ない場合は `sudo apt update && sudo apt install -y git` でインストール

**2. 作業フォルダの規約（重要）**

今後 `git clone` で取得するリポジトリはすべて **`~/dev/` 配下に統一して置きます**。

```
   🐧 WSL 内のホーム
   /home/<あなた>/                      = ~/
       └── dev/                         ← 全プロジェクトの置き場（次フェーズで作成）
            ├── ai-starter-kit/         ← 本キット
            ├── （他のプロジェクト）/    ← 将来増えてもここ
            └── ...
```

   - **WSL 内ホーム配下なので動作が速い**（`/mnt/c/...` に置くと激遅）
   - **`dev/` に集約することで clone したプロジェクトが一目で分かる**
   - `dev/` フォルダの作成と最初の `git clone` は **次フェーズ（ハンズオン）の最初のステップ** で行います。本ガイドではここで clone するためのテストは行いません

**3. プライベートリポジトリ（社内配布など）にアクセスする場合**

   - 配布元から **HTTPSのURL + アクセストークン** または **SSH鍵** の方法を指定されます
   - HTTPSの場合：clone時にユーザー名とトークン（パスワード代わり）を求められる
   - SSHの場合：別途 SSH鍵を生成して GitHub等に登録する必要があります（ハンズオン側のトラブルシューティング参照）

---

## ⑥ Python（WSL内）の存在確認 【必須】

> 📌 **このガイドでは Python は「存在確認」のみ行います。**
> Python 3 本体および開発用 apt パッケージ（`python3-pip` / `python3-dev` / `build-essential` 等）の追加導入は、**ハンズオン側で `git clone` するリポジトリ同梱の `src/scripts/setup-python.sh`** が自動で実施します。本リポジトリでは仮想環境（venv／uv 等）を意図的に使わない方針のため、`python3-venv` は導入されません。

WSLに接続した状態のVSCodeターミナルで実行します。

```bash
python3 --version
```

   - `Python 3.x.x` と表示されればOK（Ubuntuには通常プリインストール）
   - 万一「コマンドが見つかりません」が出た場合のみ、次を実行：

```bash
sudo apt update
sudo apt install -y python3
```

これで⑥は完了です。本ガイドのゴール到達です 🎉

### Phase 1 完了チェック（先に進む前にこれが全部 OK か確認）

```
   ✅ チェック項目                    やり方                確認の合図
   ──────────────────────────  ────────────────────  ─────────────────────
   [ ] WSL がインストール済み   PowerShell で         Ubuntu が表示される
                               wsl -l -v            

   [ ] VSCode がインストール済み スタートメニューで    起動できる
                               "Visual Studio Code"

   [ ] WSL 拡張が入っている     VSCode 左下に         緑の「><」がある
                               アイコンがあるか

   [ ] WSL に接続できる         VSCode 左下「><」→   左下が
                               "Connect to WSL"     "WSL: Ubuntu" になる

   [ ] Git が使える             WSL ターミナルで      git version x.y.z
                               git --version        が表示される

   [ ] Python 3 が使える        WSL ターミナルで      Python 3.x.x
                               python3 --version    が表示される
   
   全部 ✓ → 02_ハンズオン.md へ進む
   1 つでも ✗ → そのステップに戻ってやり直す
```

---

## 次のステップ → ハンズオン

ここから先は **[`02_ハンズオン.md`](./02_ハンズオン.md)** に進んでください。

ハンズオンでは:

1. リポジトリを `git clone`
2. `src/scripts/setup-*.sh` を必要分実行（Claude Code / Codex CLI / Python パッケージ / VSCode 拡張 / Docker など）
3. 動作確認
4. AI ツールを起動して実作業に入る

…までを扱います。

---

## トラブルシューティング（環境構築フェーズ）

| 症状 | 対処法 |
| --- | --- |
| `wsl --install` で「ヘルプテキスト」が出る | すでにWSLがインストール済み。`wsl --list --online` で利用可能なディストロを確認し、`wsl --install -d Ubuntu` を実行 |
| インストールが0%で止まる | `wsl --install --web-download -d Ubuntu` でやり直し |
| VSCodeの左下に「><」が出ない | WSL拡張機能がインストールできていない。③をやり直す |
| WSL接続後にコマンドが動かない | 一度 `wsl --shutdown` してから、VSCodeを再起動して接続し直す |
| Ubuntuのパスワードを忘れた | PowerShellで `wsl -u root passwd ユーザー名` でリセット可能 |
| `git --version` が出ない | `sudo apt update && sudo apt install -y git` でインストール |
| `python3 --version` が出ない | `sudo apt update && sudo apt install -y python3` でインストール |

> 💡 `git clone` 以降や `setup-*.sh` 実行時のトラブルは **[`ハンズオン`](./02_ハンズオン.md)** ドキュメント末尾のトラブルシューティングを参照してください。
> 日常的な WSL 操作（起動／停止／ファイルコピー等）は **[`WSL 操作ガイド`](./03_WSL操作.md)** にまとめています。

---

## 参考リンク

- WSL公式ドキュメント：https://learn.microsoft.com/ja-jp/windows/wsl/install
- VSCode公式：https://code.visualstudio.com/
- VSCode WSL拡張機能：https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-wsl
